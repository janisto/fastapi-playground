# AGENTS.md

Instructions for contributors and coding agents working in this FastAPI repository.

## Working principles

- Inspect the relevant existing path before changing it. Reuse local patterns and utilities before adding abstractions.
- Prefer the smallest complete change that fixes the behavior. Do not add compatibility aliases or deprecated shims.
- For architecture, data, security, or production-impacting changes, identify what breaks if the change fails.
- Keep code, tests, docs, and commits free of progress narration, autogenerated banners, emojis, speculative TODOs, and
  comments that restate the code.
- Docstrings use a multi-line form with the opening and closing `"""` on separate lines.
- Do not add dependencies without a concrete need. Use `uv`; do not mix in pip, Poetry, or ad hoc global tools.
- Never expose or log secrets, tokens, request bodies, profile data, credentials, or other PII.
- Preserve user changes and unrelated worktree edits. Remove temporary artifacts before finishing.
- Validate narrowly first, then run broader checks for shared or production-sensitive changes.

## Repository and tooling

- Python 3.14+
- FastAPI and Pydantic v2
- Firebase Admin SDK and async Firestore
- `fastapi-problem` for RFC 9457 Problem Details
- `fastapi-request-observability` for request context and access logs
- JSON and CBOR representations
- `uv` for dependencies, Ruff for lint/format, ty for types, pytest for tests, and `just` for workflows

Run `just` to list recipes. The main gates are:

```bash
just lint
just typing
just typing-functions
just test
just test-functions
just check
```

`just check` verifies both projects, including the exact Functions requirements export. `just update` upgrades the root
and Functions lockfiles and regenerates `functions/requirements.txt`. Both project manifests enforce the minimum
supported uv version while allowing newer releases. The Docker builder stays exactly versioned for reproducible image
builds. When a newer uv release changes the export, regenerate it and review the diff; update the minimum version only
when the repository starts relying on that release.

## Project structure and boundaries

- `app/api/`: HTTP transport and route metadata
- `app/services/`: business rules and Firestore operations
- `app/models/`: separate request and response models plus shared constrained types
- `app/core/`: configuration, Firebase lifecycle, logging, negotiation, OpenAPI helpers, and exception handling
- `app/middleware/`: application-specific ASGI middleware
- `app/exceptions/`: domain Problem Details exceptions
- `tests/unit/`: isolated helpers, models, middleware, auth, and service behavior
- `tests/integration/`: composed FastAPI and middleware behavior
- `tests/e2e/`: real local Firebase emulator behavior
- `functions/`: independent Firebase Functions project and tests

Keep routers thin. Inject services through typed aliases in `app/dependencies.py`. Production code must not contain test
flags, fakes, environment backdoors, or mock-only branches.

Business routers own their complete `/v1/...` prefix and are exported through `business_routers` in `app/api/__init__.py`.
This keeps FastAPI route metadata, OpenAPI paths, and observability `path_template` values aligned. Health and schema
discovery remain unversioned.

## Application composition

`app/main.py` builds `fastapi_app` first, then exports the outer `app`. Use `fastapi_app` for dependency overrides and
OpenAPI inspection; send test and production traffic to `app`.

The request flow is:

```text
RequestContext -> SecurityHeaders -> AccessLog -> CORS (when configured) -> BodySizeLimit -> FastAPI
```

Keep request context outermost so every response, including recovery and limit responses, receives `X-Request-ID`.
Keep access logging outside FastAPI recovery so failures emit one correlated access record.

Use the lifespan context manager for startup and shutdown. Configure logging and initialize Firebase on startup. Close
the async Firestore client synchronously in a `finally` block; the supported SDK's `AsyncClient.close()` is not awaitable.

Cloud Run terminates TLS before proxying HTTP to the container. The image runtime trusts Cloud Run forwarded headers so
the application sees the original HTTPS scheme for HSTS and URL behavior. Preserve the established image, build, and
deployment conventions. Change them only when a replacement is materially better and verified against both the
repository and active deployment behavior.

## HTTP contract

- Paths have no trailing slash; redirects are disabled.
- Return resources directly without envelopes.
- GET and PATCH normally return 200, persistent POST returns 201 plus `Location`, and DELETE returns 204 without a body.
- Use stable operation IDs in `<resource>_<action>` form.
- Use precise return annotations. Use explicit `response_model` only when response-model options are required.
- Endpoint docstrings describe behavior and notable failures; decorators contain accurate summaries, descriptions, and
  reachable statuses.
- Use `Literal` for fixed value sets and shared constrained aliases for reusable validation.
- An invalid pagination cursor is a malformed parameter and returns 400, not 422.

Expected domain failures use exceptions derived from the narrowest `fastapi-problem` class. Re-raise expected domain or
transport exceptions. Log unexpected failures with safe structured context and return a generic 500.

### Representations and negotiation

- JSON is the default response representation.
- CBOR is returned only when explicitly requested through `Accept`.
- Client quality weights are honored; JSON is the server preference on ties.
- An explicit `Accept` value that allows neither supported representation returns 406.
- Request bodies use `Content-Type: application/json` or `application/cbor`; unsupported body types return 415.
- Errors use `application/problem+json` or `application/problem+cbor`.
- The global request-size guard returns 413 without reading the remaining unbounded body.

### Problem Details and schema discovery

Problem bodies follow RFC 9457. Validation errors use an `errors` array with dot-notation locations. Echo only bounded
scalar invalid values, redact compound secret field names, and never attach a container that could be the full request
body.

Modeled responses advertise a relative schema link:

```http
Link: </schemas/Model.json>; rel="describedBy"
```

Do not add `$schema` to API response instances. `$schema` identifies the JSON Schema dialect and belongs on the
standalone documents served by `/schemas/{Model}.json`. Reusable OpenAPI response content, media types, and headers live
in `app/core/openapi.py`. Keep runtime behavior, OpenAPI, schema discovery, and contract tests aligned.

Document response headers that runtime code emits, including `X-Request-ID`, `Link`, `Location`, `WWW-Authenticate`, and
`Retry-After`. Document both JSON and CBOR only where both are implemented.

## Pydantic models and timestamps

- Request and response models are separate. Never reuse a response model as writable input.
- Request models use `ConfigDict(extra="forbid")`.
- Response models do not inherit strict request bases.
- Use `model_dump()`; use `exclude_unset=True` for PATCH.
- PATCH fields may be omitted but explicit null is rejected unless a domain explicitly defines null semantics.
- Use shared `UtcDatetime`, `NormalizedEmail`, and `Phone` aliases from `app/models/types.py` where applicable.
- Public timestamps are UTC ISO 8601 with explicit millisecond precision, for example `2025-01-15T10:30:00.000Z`.
- Scalar fields have useful `Field` descriptions and examples. Nested-model arrays rely on their referenced schemas.
- Do not use model-level `json_schema_extra` examples.
- Keep Firestore collection names as exported domain constants.

## Firestore, authentication, and logging

- Use `get_async_firestore_client()` and inject services rather than constructing clients in routers.
- User-owned profile documents use the authenticated Firebase UID as both ownership boundary and resource ID. Never
  accept a client-selected owner.
- Use `@firestore.async_transactional` where create-if-absent or read-modify-write semantics require atomicity.
- Preserve `created_at`; change `updated_at` only on a mutation.
- Map absence and conflict to domain exceptions instead of leaking SDK results across the service boundary.
- Emit one structured audit record after a successful security-relevant mutation.
- Firebase token verification checks revocation. Credential failures return 401 with `WWW-Authenticate: Bearer`; key
  retrieval or verification dependency failures return 503 with `Retry-After`.
- Use standard-library module loggers in the FastAPI app and structured `extra={...}` context. Use
  `logger.exception()` only inside exception handlers. Do not interpolate context into log messages.
- Request trace fields represent valid incoming W3C Trace Context correlation. The middleware does not create spans.

## Configuration and CORS

`FIREBASE_PROJECT_ID` is required. `ENVIRONMENT` is one of `development`, `test`, or `production`.
`MAX_REQUEST_SIZE_BYTES` must be positive. Invalid JSON-array syntax in `CORS_ORIGINS` is a configuration error, not a
literal origin fallback.

Use Application Default Credentials or Secret Manager in deployed environments. Service-account files remain ignored.
Do not commit project IDs, numbers, account emails, URLs, registry paths, revision IDs, digests, credentials, or token
values to public docs or configuration examples.

## Firebase Function

`functions/` is an independent Python 3.14 project. Its `dad_joke` HTTP function is GET-only and private because every
successful call can consume Vertex AI quota. Preserve `invoker="private"`; intended callers need `roles/run.invoker` on
the backing Cloud Run service and must send an ID token. Keep model output strictly validated and return sanitized 503
or 500 responses. Logs may include stable status or exception type, never prompts, generated output, exception strings,
credentials, or user data.

Unit tests in `functions/tests/` must isolate Genkit reflection and GCP telemetry and must not call Vertex AI. Firebase
deploys from the exact runtime-only `functions/requirements.txt` export; do not hand-edit it.

## Testing

Use the narrowest meaningful boundary:

- Unit tests for pure functions, models, middleware, auth branches, service behavior, and Firestore fakes.
- Integration tests for routing, middleware order, request IDs, negotiation, Problem Details, OpenAPI, and headers.
- E2E tests for real Firebase SDK and transaction behavior against local emulators.
- Functions tests for handler behavior and deployment manifest controls.

Assert observable behavior rather than implementation trivia: exact status and media type, relevant headers, decoded
shape, service calls, error redaction, route metadata, and absence of sensitive output. Prefer parametrization for input
matrices and `AsyncMock` assertions for async calls. No real network or Firestore access in unit or integration tests.
The emulator recipe always uses the synthetic `demo-test` project; do not point E2E tests at a deployed project.

The main commands are:

```bash
just test-unit
just test-integration
just test-functions
just test
just cov
just emulators
just test-e2e
just test-all
```

Run emulator E2E tests when Firestore transaction or SDK semantics change. For container-sensitive changes, verify the
actual image with `just container-build`, `just container-up`, `just container-logs`, and `just container-down`.

## Documentation alignment

Keep `README.md`, `GCP.md`, and `AGENTS.md` mutually consistent when behavior, configuration, commands, deployment
guidance, or contributor workflows change:

- `README.md` is the concise public overview.
- `GCP.md` is intentionally detailed, project-agnostic Google Cloud and Firebase onboarding. Preserve useful setup and
  troubleshooting guidance.
- `AGENTS.md` is the authoritative repository workflow and engineering guide.

Treat all three as public. Use placeholders and preserve established build/image conventions. Also update
`functions/README.md` for Function-specific contract, IAM, dependency, or emulator changes.

The workspace keeps equivalent Firebase and Cloud Logging MCP configurations in `.vscode/mcp.json` and
`.codex/config.toml`. Both use shell-provided `GOOGLE_CLOUD_PROJECT` and a short-lived
`GOOGLE_CLOUD_ACCESS_TOKEN`. Keep the gcloud default project and ADC quota project aligned, restart clients after token
refresh, and never commit an expanded token.

Cloud Logging MCP is available to investigate deployed behavior. Use it when production evidence is relevant: select
the appropriate resource and time range, constrain queries to the service or function under investigation, and inspect
request IDs, severity, route metadata, and trace correlation. Do not copy sensitive log payloads into code, docs, tests,
or PR descriptions. Repository evidence and deployed evidence answer different questions; report which one supports a
finding.

## Agent skills

Task-specific portable skills live under `.agents/skills/`. Keep the set small; a skill should encode a recurring,
repository-specific workflow rather than restate this file.

| Skill | Use when |
|---|---|
| `fastapi-endpoint` | Creating or changing a route and its runtime behavior |
| `firestore-service` | Changing async Firestore persistence or transaction semantics |
| `openapi-contract` | Changing routes, schemas, errors, media types, headers, or API docs |
| `pytest-testing` | Writing or reviewing app, emulator, or Functions tests |
| `readme-maintenance` | Auditing public README claims after repository changes |

These five skills cover distinct recurring boundaries and should remain separate. Do not add generic lint, review, GCP,
or logging skills whose workflow is already captured by `just`, this file, or product tools. The read-only GitHub
Copilot security profile remains at `.github/agents/security-review.agent.md` because that product discovers custom
agents there.

The repository uses the standard [AGENTS.md](https://github.com/agentsmd/agents.md) format and the
[Agent Skills specification](https://agentskills.io/specification). If a skill name, description, or default prompt
changes, keep its metadata aligned.

## Completion checklist

Before declaring code work complete:

1. Inspect the final diff for accidental scope, stale comments, secrets, and unrelated formatting.
2. Run focused tests, then the applicable repository gates.
3. Run `just lint` and `just typing` after code edits; include Functions checks when that project or shared automation
   changed.
4. Verify docs and generated OpenAPI claims against current behavior.
5. Report implementation, validation results, and any remaining production or external risks clearly.
