[project]
name = "fastapi-playground"
version = "0.1.0"
description = "FastAPI playground for just, uv, ty and Ruff"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "firebase-admin>=7.1.0",
    "pydantic>=2.11.7",
    "pydantic-settings>=2.10.1",
    "uvicorn[standard]>=0.35.0",
    "python-jose[cryptography]>=3.5.0",
    "httpx>=0.28.1",
]

[dependency-groups]
dev = [
    "pytest>=8.4.1",
    "pytest-asyncio>=1.1.0",
    "pytest-cov>=6.2.1",
    "pytest-httpx>=0.35.0",
    "ruff>=0.12.11",
    "ty>=0.0.1a19",
]

[tool.ruff]
# Allow lines to be as long as 120.
line-length = 120

[tool.ruff.lint]
# Add type annotation and FastAPI rules, plus pyupgrade to modernize typing
extend-select = ["ANN", "FAST", "UP"]

[tool.ty.environment]
# Multiple directories (priority order)
root = ["./app", "./tests"]

[tool.ty.rules]
unresolved-attribute = "ignore"
unresolved-import = "ignore"

[tool.pytest.ini_options]
# addopts = "--maxfail=1 --cov=app"

[tool.coverage.run]
branch = true
parallel = true
source = ["app", "tests"]

[tool.coverage.report]
show_missing = true
skip_covered = true
omit = ["app/**/asgi.py"]
