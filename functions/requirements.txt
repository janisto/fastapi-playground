## Production dependencies for Cloud Functions (Python 3.14)
## Format: package[extras]==PIN  (pin where stability matters) or >= for minimum.
## Keep this file in sync with pyproject.toml. Prefer pinning to reduce cold start variance.

# Core Firebase / Google SDKs
firebase-admin>=7.1.0
firebase-functions>=0.5.0

# Generative AI client
google-genai>=1.56.0
